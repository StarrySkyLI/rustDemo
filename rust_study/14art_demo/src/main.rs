// use art_demo::kinds::PrimaryColor;
// use art_demo::utils::mix;
use art_demo::PrimaryColor;
use art_demo::mix;
fn main() {
    let red = PrimaryColor::Red;
    let yellow = PrimaryColor::Yellow;
    mix(red, yellow);
}